# Secrets — never committed (real keys live in .env only)
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
dist/
*.tsbuildinfo

# Local data (SQLite incl. WAL/SHM, logs)
*.sqlite
*.sqlite3
*.db
server/data/
logs/

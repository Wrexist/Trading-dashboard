# Secrets — never committed (real keys live in .env only)
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
dist/
*.tsbuildinfo

# Local data (SQLite, logs)
*.sqlite
*.sqlite3
*.db
logs/
